fn main() {}
// use num_bigint::BigInt;
//
// use octonion::consts::M;
// use octonion::utils::inverse;
//
// fn main() {
//     let mut g0: BigInt = BigInt::from(1) * inverse(BigInt::from(2), M.clone());
//     let mut g1: BigInt = rng.sample(RandomBits::new(M9689_BITS)) % &*M;
//     let mut g2: BigInt = rng.sample(RandomBits::new(M9689_BITS)) % &*M;
//     let mut g3: BigInt = rng.sample(RandomBits::new(M9689_BITS)) % &*M;
//     let mut g4: BigInt = rng.sample(RandomBits::new(M9689_BITS)) % &*M;
//     let mut g5: BigInt = rng.sample(RandomBits::new(M9689_BITS)) % &*M;
//     let mut g6: BigInt = rng.sample(RandomBits::new(M9689_BITS)) % &*M;
//     if g1 < BigInt::default() {
//         g1 += &*M;
//     }
//     if g2 < BigInt::default() {
//         g2 += &*M;
//     }
//     if g3 < BigInt::default() {
//         g3 += &*M;
//     }
//     if g4 < BigInt::default() {
//         g4 += &*M;
//     }
//     if g5 < BigInt::default() {
//         g5 += &*M;
//     }
//     if g6 < BigInt::default() {
//         g6 += &*M;
//     }
//     let mut h0: BigInt = BigInt::from(0);
//     let mut h1: BigInt = rng.sample(RandomBits::new(M9689_BITS)) % &*M;
//     let mut h2: BigInt = rng.sample(RandomBits::new(M9689_BITS)) % &*M;
//     let mut h3: BigInt = rng.sample(RandomBits::new(M9689_BITS)) % &*M;
//     let mut h4: BigInt = rng.sample(RandomBits::new(M9689_BITS)) % &*M;
//     let mut h5: BigInt = rng.sample(RandomBits::new(M9689_BITS)) % &*M;
//     let mut h6: BigInt = rng.sample(RandomBits::new(M9689_BITS)) % &*M;
//     if h1 < BigInt::default() {
//         h1 += &*M;
//     }
//     if h2 < BigInt::default() {
//         h2 += &*M;
//     }
//     if h3 < BigInt::default() {
//         h3 += &*M;
//     }
//     if h4 < BigInt::default() {
//         h4 += &*M;
//     }
//     if h5 < BigInt::default() {
//         h5 += &*M;
//     }
//     if h6 < BigInt::default() {
//         h6 += &*M;
//     }
// }
